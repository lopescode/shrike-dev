{
  "name": "shrike-dev",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "gen-transfers": "ts-node-dev --respawn --transpile-only src/gen-transfers.ts",
    "gen-balances": "ts-node-dev --respawn --transpile-only src/gen-balances.ts",
    "calc-balances": "ts-node-dev --respawn --transpile-only src/calc-balances.ts",
    "calc-total-gas-claimed": "ts-node-dev --respawn --transpile-only src/calc-total-gas-claimed.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "bs58": "^6.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  }
}
